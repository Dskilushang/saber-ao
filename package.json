{
  "name": "saber-ao",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "npx cap init \"SABER AO\" com.dskilushang.saberao --web-dir=dist",
    "cap:sync": "npm run build && npx cap copy",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "vite": "^5.1.0"
  }
}
