{
  "name": "saber-ao",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "npx cap init \"SABER AO\" com.dskilushang.quizangola --web-dir=dist",
    "cap:sync": "npm run build && npx cap copy",
    "cap:add:android": "npx cap add android"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "vite": "^5.1.0",
    "@vitejs/plugin-react": "^4.0.0",
    "@capacitor/cli": "^5.0.0"
  }
}
